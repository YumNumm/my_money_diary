import 'package:flutter/material.dart';

class IcReaderScreen extends StatelessWidget {
  const IcReaderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ICリーダー'),
      ),
      body: const Placeholder(),
    );
  }
}
